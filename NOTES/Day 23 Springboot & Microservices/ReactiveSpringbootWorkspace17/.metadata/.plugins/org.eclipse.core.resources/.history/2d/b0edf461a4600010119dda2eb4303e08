package training.iqgateway.moviecatalogservice;

import java.util.Arrays;
import java.util.Collections;
import java.util.List;
import java.util.stream.Collectors;

import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import training.iqgateway.moviecatalogservice.model.CatalogItems;
import training.iqgateway.moviecatalogservice.model.Rating;

@RestController
@RequestMapping("/catalog")
public class MovieCatalogResource {

	@GetMapping("/{userId}")
	public List<CatalogItems> getCatalog(@PathVariable String userId) {
		
		List<Rating> ratings = Arrays.asList(new Rating("1001", 5), new Rating("1002", 4));

		return ratings.stream().map(rating -> new CatalogItems("KGF Chapter-1", "Awesome", 5))
				.collect(Collectors.toList());

		// return Collections.singletonList(new CatalogItems("Chennai Express", "Superbbb", 5));
	}
}